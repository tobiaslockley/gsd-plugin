---
phase: 03-plugin-packaging-and-memory
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - README.md
  - .claude-plugin/marketplace.json
  - .mcp.json
  - hooks/hooks.json
  - .claude-plugin/plugin.json
  - migrations/
  - bin/gsd-tools.cjs
  - skills/
  - .planning/STATE.md
autonomous: true
requirements:
  - PLUG-02
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - "A user can install GSD in one step through the plugin flow without manually creating `~/.claude/get-shit-done`, `.mcp.json`, or `~/.claude/settings.json` entries"
    - "Legacy GSD runtime dependencies are either migrated, tombstoned, or explicitly documented for cleanup"
    - "Migration guidance is concrete enough to move existing users off the legacy install safely"
    - "Phase 3 ends with a zero-blocking legacy-path audit over packaged runtime surfaces"
  artifacts:
    - path: "README.md"
      provides: "Single-step install and migration guidance"
      contains: "claude plugin install gsd"
    - path: "migrations/"
      provides: "Optional legacy cleanup or migration helper"
      contains: ""
    - path: ".planning/STATE.md"
      provides: "Updated planning state after Phase 3 planning/execution prep"
      contains: "Phase: 3"
  key_links:
    - from: "README.md"
      to: ".claude-plugin/plugin.json"
      via: "documents the packaged install path"
      pattern: "plugin"
    - from: "migrations/"
      to: "hooks/hooks.json"
      via: "cleanup removes old user settings dependency after packaged hooks exist"
      pattern: "hook"
---

<objective>
Finish Phase 3 by providing a safe migration path from the legacy `~/.claude/get-shit-done` install, documenting the new single-step install flow, and proving the plugin can stand on its own.

Purpose: Phase 3 is not complete until old users can move safely and the new plugin install path replaces the old setup instructions end to end.

Output: installation/migration docs, optional cleanup helper, and a final audit showing no blocking dependency on the legacy install.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
@.planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
@.planning/phases/03-plugin-packaging-and-memory/03-02-PLAN.md
@.planning/phases/03-plugin-packaging-and-memory/03-03-PLAN.md
@.planning/phases/03-plugin-packaging-and-memory/03-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the real plugin distribution and install contract</name>
  <files>
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
    skills/
    bin/gsd-tools.cjs
  </files>
  <read_first>
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
    commands/plugin/ValidatePlugin.tsx
    utils/plugins/marketplaceManager.ts
    utils/plugins/schemas.ts
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
    .planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
  </read_first>
  <action>
Create the repo-owned distribution contract that makes single-step plugin install real in practice.

Add `.claude-plugin/marketplace.json` with a concrete entry for `gsd` so the repo can be published or loaded as a real plugin source rather than only a local manifest. The marketplace entry must define the plugin name, source repo/path, and any metadata required by the existing marketplace loader.

At the same time, explicitly retire the legacy update/install paths required by the locked decisions in `03-CONTEXT.md`:
- make `/gsd:update` a deprecation path that tells users plugin-managed updates replace it, or remove the packaged command entirely if that is cleaner
- replace `get-shit-done-cc` as the documented install/update path with the plugin distribution path

The end state must support a concrete install contract grounded in repo assets:
- marketplace entry exists
- plugin manifest exists
- the exact supported install command is `claude plugin install gsd`
- update flow points at plugin-managed updates instead of `/gsd:update`
  </action>
  <verify>
    <automated>test -f .claude-plugin/marketplace.json && rg -n "\"gsd\"|plugin|github|source|version" .claude-plugin/marketplace.json .claude-plugin/plugin.json && rg -n "/gsd:update|plugin-managed update|get-shit-done-cc" skills bin/gsd-tools.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/marketplace.json` exists
    - `.claude-plugin/marketplace.json` contains an entry for `gsd`
    - The repo contains a concrete plugin source/distribution contract, not just a local plugin manifest
    - The distribution contract explicitly supports `claude plugin install gsd`
    - `/gsd:update` is explicitly retired, redirected, or removed from the packaged GSD flow
    - `get-shit-done-cc` is explicitly retired as the install/update path
  </acceptance_criteria>
  <done>GSD has a real plugin distribution/install contract and no longer depends on legacy installer/update mechanisms.</done>
</task>

<task type="auto">
  <name>Task 2: Document the new single-step install and legacy migration flow</name>
  <files>
    README.md
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
  </files>
  <read_first>
    README.md
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Update `README.md` so the primary install path is the exact locked command:
`claude plugin install gsd`

Add a migration section with concrete steps for existing users who still have:
- `~/.claude/get-shit-done/`
- repo-local `.mcp.json` GSD entries
- `~/.claude/settings.json` hook entries pointing at old GSD scripts
- `get-shit-done-cc` in older installation docs
- `/gsd:update` in older workflow docs

The migration section must tell the user exactly what to remove, what the plugin now replaces, and how to verify the plugin-managed setup is active.
  </action>
  <verify>
    <automated>rg -n "claude plugin install gsd|~/.claude/get-shit-done|\\.mcp\\.json|settings\\.json|get-shit-done-cc|/gsd:update" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `claude plugin install gsd`
    - `README.md` contains a migration section mentioning `~/.claude/get-shit-done`
    - `README.md` contains a migration section mentioning `.mcp.json`
    - `README.md` contains a migration section mentioning `~/.claude/settings.json`
    - `README.md` explicitly retires `get-shit-done-cc`
    - `README.md` explicitly retires `/gsd:update`
  </acceptance_criteria>
  <done>Installation and migration are documented as a real plugin flow with explicit retirement of the legacy installer and update command.</done>
</task>

<task type="auto">
  <name>Task 3: Add a legacy cleanup or tombstone helper for old installs</name>
  <files>
    migrations/
    bin/gsd-tools.cjs
  </files>
  <read_first>
    migrations/
    bin/gsd-tools.cjs
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Create one concrete migration helper under `migrations/` or wire an equivalent `bin/gsd-tools.cjs` subcommand that audits and optionally cleans up the exact legacy surfaces found in research:
- `~/.claude/get-shit-done/`
- GSD entries in repo `.mcp.json`
- GSD hook entries in `~/.claude/settings.json`

If automatic deletion is too risky, implement a tombstone/audit mode that prints the exact paths and JSON keys to remove. The helper must not silently delete data.
  </action>
  <verify>
    <automated>rg -n "get-shit-done|\\.mcp\\.json|settings\\.json|audit|migrate|cleanup" migrations bin/gsd-tools.cjs</automated>
  </verify>
  <acceptance_criteria>
    - A migration helper exists under `migrations/` or as a concrete `bin/gsd-tools.cjs` command
    - The helper references `~/.claude/get-shit-done`
    - The helper references `.mcp.json`
    - The helper references `settings.json`
    - The helper references `get-shit-done-cc` or `/gsd:update` when describing retirement/cleanup scope
    - The helper does not perform silent destructive cleanup without an explicit user action
  </acceptance_criteria>
  <done>Legacy GSD installs can be audited and cleaned up with an explicit, non-silent migration helper.</done>
</task>

<task type="auto">
  <name>Task 4: Run the final zero-blocking legacy dependency audit</name>
  <files>
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
    hooks/hooks.json
    mcp/server.cjs
    skills/
    agents/
    bin/gsd-tools.cjs
    README.md
  </files>
  <read_first>
    .claude-plugin/marketplace.json
    .claude-plugin/plugin.json
    hooks/hooks.json
    mcp/server.cjs
    skills/
    agents/
    bin/gsd-tools.cjs
    README.md
  </read_first>
  <action>
Run a final audit that distinguishes between:
- allowed documentation references describing migration away from legacy paths
- disallowed runtime references that still require the old install

The disallowed runtime surfaces are:
- `.claude-plugin/plugin.json`
- `hooks/hooks.json`
- `mcp/server.cjs`
- `skills/`
- `agents/`
- `bin/gsd-tools.cjs`

The audit must fail if any of those runtime surfaces still reference:
- `~/.claude/get-shit-done`
- `/Users/.../.claude/get-shit-done`
- `~/.claude/hooks`
- legacy `.mcp.json` bootstrap as a runtime requirement
  </action>
  <verify>
    <automated>rg -n "~/.claude/get-shit-done|/Users/.*/\\.claude/get-shit-done|~/.claude/hooks|\\.mcp\\.json|get-shit-done-cc|/gsd:update" .claude-plugin/marketplace.json .claude-plugin/plugin.json hooks/hooks.json mcp/server.cjs skills agents bin/gsd-tools.cjs README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "~/.claude/get-shit-done|/Users/.*/\\.claude/get-shit-done|~/.claude/hooks|get-shit-done-cc" .claude-plugin/marketplace.json .claude-plugin/plugin.json hooks/hooks.json mcp/server.cjs skills agents bin/gsd-tools.cjs` returns no matches
    - Any remaining `.mcp.json` mentions are documentation-only and not runtime requirements
    - Any remaining `/gsd:update` mentions are documentation-only deprecation notes and not live runtime requirements
    - `README.md` may mention legacy paths only in migration guidance, not as the primary install path
  </acceptance_criteria>
  <done>Phase 3 closes with a clean runtime audit: legacy paths appear only in migration guidance, not in live runtime surfaces.</done>
</task>

</tasks>

<verification>
- `rg -n "claude plugin install gsd" README.md`
- `test -f .claude-plugin/marketplace.json`
- `rg -n "~/.claude/get-shit-done|~/.claude/hooks|get-shit-done-cc" .claude-plugin/marketplace.json .claude-plugin/plugin.json hooks/hooks.json mcp/server.cjs skills agents bin/gsd-tools.cjs` returns no matches
- `rg -n "migrate|cleanup|audit" migrations bin/gsd-tools.cjs`
</verification>

<success_criteria>
Existing users can move safely off the legacy install, new users have a single install command, and no blocking runtime dependency on `~/.claude/get-shit-done` remains.
</success_criteria>
