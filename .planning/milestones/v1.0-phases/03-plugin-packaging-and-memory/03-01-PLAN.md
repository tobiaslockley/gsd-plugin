---
phase: 03-plugin-packaging-and-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - package.json
  - bin/gsd-tools.cjs
  - bin/lib/core.cjs
  - bin/lib/init.cjs
  - utils/plugins/schemas.ts
  - utils/plugins/pluginLoader.ts
  - utils/plugins/validatePlugin.ts
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02

must_haves:
  truths:
    - "GSD has a repo-owned `.claude-plugin/plugin.json` that Claude Code can validate and install as a plugin"
    - "The plugin manifest declares the packaged GSD metadata and MCP server entrypoint without depending on `~/.claude/get-shit-done`"
    - "Runtime helpers can resolve the plugin root and plugin data directory when running from an installed plugin cache"
    - "Plugin validation succeeds against the repo-owned manifest and packaged component paths"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Canonical plugin manifest for GSD"
      contains: "\"name\""
    - path: "bin/gsd-tools.cjs"
      provides: "Packaged CLI/runtime entrypoint that can run from plugin install paths"
      contains: "CLAUDE_PLUGIN_ROOT"
    - path: "utils/plugins/validatePlugin.ts"
      provides: "Validation path for `.claude-plugin/plugin.json` packaging flow"
      contains: ".claude-plugin"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "utils/plugins/schemas.ts"
      via: "PluginManifestSchema validation"
      pattern: "PluginManifestSchema"
    - from: ".claude-plugin/plugin.json"
      to: "utils/plugins/pluginLoader.ts"
      via: "plugin loader reads `.claude-plugin/plugin.json` from plugin root"
      pattern: "\\.claude-plugin"
    - from: "bin/gsd-tools.cjs"
      to: ".claude-plugin/plugin.json"
      via: "runtime path resolution uses packaged plugin root/data"
      pattern: "CLAUDE_PLUGIN_ROOT"
---

<objective>
Create the repo-owned GSD plugin scaffold so Claude Code can install GSD as a standard plugin with bundled MCP metadata and packaged runtime entrypoints.

Purpose: This is the packaging foundation for Phase 3. It establishes the plugin manifest, packaged runtime path model, and validation path before any skill, hook, MCP, or memory migration happens.

Output: `.claude-plugin/plugin.json` exists, points at packaged assets, validates successfully, and runtime helpers no longer assume `~/.claude/get-shit-done` is the content root.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
@.planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical GSD plugin manifest and packaging metadata</name>
  <files>
    .claude-plugin/plugin.json
    package.json
  </files>
  <read_first>
    utils/plugins/schemas.ts
    utils/plugins/pluginLoader.ts
    utils/plugins/validatePlugin.ts
    .planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Create `.claude-plugin/plugin.json` as the canonical GSD manifest with concrete metadata and packaged component declarations. Use repo-relative plugin paths only. Do not reference `~/.claude/get-shit-done`, `.mcp.json`, or `execution_context` files from the manifest.

The manifest must include:
- `"name": "gsd"`
- A real `"version"` sourced from `package.json`
- `"description"` describing GSD as a Claude Code workflow plugin
- `"mcpServers"` with server name `gsd`, `"type": "stdio"`, `"command": "node"`, and args pointing at `"${CLAUDE_PLUGIN_ROOT}/mcp/server.cjs"`
- Any required command/skill/agent path declarations only if the default `skills/`, `agents/`, and `hooks/hooks.json` discovery is insufficient

Update `package.json` so the repo owns plugin packaging metadata and exposes any build/validate scripts needed for Phase 3, including a script that validates `.claude-plugin/plugin.json` through the existing plugin validator.

Create one exact repo-owned validation command in `package.json` named:
`validate:gsd-plugin`

That command must execute the real plugin validator against `.claude-plugin/plugin.json`. Do not treat grep output as validation success.

Keep the plugin layout aligned to the researched structure:
`.claude-plugin/plugin.json`, `skills/`, `agents/`, `hooks/hooks.json`, `bin/`, `mcp/`, `templates/`, and `references/`.
  </action>
  <verify>
    <automated>test -f .claude-plugin/plugin.json && rg -n "\"mcpServers\"|\"name\"|CLAUDE_PLUGIN_ROOT" .claude-plugin/plugin.json package.json</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` exists
    - `.claude-plugin/plugin.json` contains `"name": "gsd"`
    - `.claude-plugin/plugin.json` contains `"mcpServers"`
    - `.claude-plugin/plugin.json` contains `"${CLAUDE_PLUGIN_ROOT}/mcp/server.cjs"`
    - `package.json` exists and contains a version string used by the plugin manifest
    - No line in `.claude-plugin/plugin.json` contains `~/.claude/get-shit-done`
  </acceptance_criteria>
  <done>GSD has a repo-owned plugin manifest with packaged MCP metadata and installable plugin metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Make packaged runtime path resolution work from plugin cache installs</name>
  <files>
    bin/gsd-tools.cjs
    bin/lib/core.cjs
    bin/lib/init.cjs
  </files>
  <read_first>
    bin/gsd-tools.cjs
    bin/lib/core.cjs
    bin/lib/init.cjs
    utils/hooks.ts
    utils/plugins/mcpPluginIntegration.ts
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Add a single packaged-runtime resolution path that supports these concrete cases:
1. Running from the repo checkout during development
2. Running from Claude Code's plugin cache via `${CLAUDE_PLUGIN_ROOT}`
3. Reading persistent mutable plugin state via `${CLAUDE_PLUGIN_DATA}`

Update `bin/gsd-tools.cjs`, `bin/lib/core.cjs`, and `bin/lib/init.cjs` to stop assuming the GSD content root is `~/.claude/get-shit-done`. Introduce a shared helper that resolves:
- plugin root
- plugin data directory
- repo root for development mode
- packaged `templates/`, `references/`, `skills/`, `agents/`, and `mcp/` paths

Use exact environment names when present:
- `CLAUDE_PLUGIN_ROOT`
- `CLAUDE_PLUGIN_DATA`

If the process is not running from a plugin install, fall back to the repo root rather than a user-home install path.
  </action>
  <verify>
    <automated>rg -n "CLAUDE_PLUGIN_ROOT|CLAUDE_PLUGIN_DATA|get-shit-done" bin/gsd-tools.cjs bin/lib/core.cjs bin/lib/init.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bin/gsd-tools.cjs` contains `CLAUDE_PLUGIN_ROOT`
    - `bin/lib/core.cjs` contains `CLAUDE_PLUGIN_ROOT`
    - `bin/lib/core.cjs` contains `CLAUDE_PLUGIN_DATA`
    - `bin/lib/init.cjs` uses packaged path helpers instead of hardcoded `~/.claude/get-shit-done`
    - `rg -n "~/.claude/get-shit-done" bin/gsd-tools.cjs bin/lib/core.cjs bin/lib/init.cjs` returns no matches
  </acceptance_criteria>
  <done>Packaged runtime helpers resolve plugin root and plugin data without depending on the legacy install tree.</done>
</task>

<task type="auto">
  <name>Task 3: Add repo-level plugin validation for GSD packaging</name>
  <files>
    utils/plugins/validatePlugin.ts
    utils/plugins/pluginLoader.ts
    package.json
  </files>
  <read_first>
    utils/plugins/validatePlugin.ts
    utils/plugins/pluginLoader.ts
    cli/handlers/plugins.ts
    .claude-plugin/plugin.json
  </read_first>
  <action>
Ensure the repo's existing plugin validation flow can validate GSD from the repository root and from `.claude-plugin/plugin.json` directly. Keep the `.claude-plugin` behavior explicit so Phase 3 execution can run one concrete validation command against the repo-owned plugin.

Add or expose one exact validation path in `package.json`, for example a script that runs the existing plugin validation handler against `.claude-plugin/plugin.json`.

Do not create a second validation mechanism. Reuse the current plugin loader and validator code paths already used by Claude Code.
  </action>
  <verify>
    <automated>npm run validate:gsd-plugin</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains a `validate:gsd-plugin` script targeting `.claude-plugin/plugin.json`
    - `utils/plugins/validatePlugin.ts` still contains `.claude-plugin` support
    - `utils/plugins/pluginLoader.ts` still loads `.claude-plugin/plugin.json`
    - `npm run validate:gsd-plugin` exits 0
    - There is one documented repo-owned way to validate the packaged GSD plugin
  </acceptance_criteria>
  <done>GSD packaging can be validated through the existing plugin loader/validator using the repo-owned plugin manifest.</done>
</task>

</tasks>

<verification>
- `test -f .claude-plugin/plugin.json`
- `rg -n "\"name\": \"gsd\"|\"mcpServers\"|CLAUDE_PLUGIN_ROOT" .claude-plugin/plugin.json`
- `rg -n "CLAUDE_PLUGIN_ROOT|CLAUDE_PLUGIN_DATA" bin/gsd-tools.cjs bin/lib/core.cjs bin/lib/init.cjs`
- `npm run validate:gsd-plugin`
- `rg -n "~/.claude/get-shit-done" .claude-plugin/plugin.json bin/gsd-tools.cjs bin/lib/core.cjs bin/lib/init.cjs` returns no matches
</verification>

<success_criteria>
GSD has a real plugin manifest, packaged runtime path resolution, and a repo-owned validation path. PLUG-01 and the packaging half of PLUG-02 are unblocked.
</success_criteria>
