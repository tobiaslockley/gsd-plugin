---
phase: 03-plugin-packaging-and-memory
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - memdir/
  - utils/claudemd.ts
  - bin/gsd-tools.cjs
  - bin/lib/state.cjs
  - commands/verify-work/
  - commands/complete-milestone/
  - skills/
autonomous: true
requirements:
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - "Phase completion writes one lean `project` memory file per completed phase into Claude Code's auto-memory system"
    - "Written memory content contains only durable phase outcome, non-obvious decisions, and surprising blocker resolution"
    - "Cross-session recall works by writing memdir-compatible files that Claude Code already auto-loads"
    - "Memory writing happens at verification/completion time rather than during every plan or hook event"
  artifacts:
    - path: "commands/verify-work/"
      provides: "Verification flow with phase-memory write step"
      contains: "memory"
    - path: "commands/complete-milestone/"
      provides: "Milestone completion flow that can finalize any remaining memory write"
      contains: "memory"
    - path: "bin/lib/state.cjs"
      provides: "State/runtime helper used to construct phase outcome memory payloads"
      contains: "phase"
  key_links:
    - from: "commands/verify-work/"
      to: "memdir/"
      via: "writes memdir-compatible project memory files"
      pattern: "project"
    - from: "commands/complete-milestone/"
      to: "utils/claudemd.ts"
      via: "relies on Claude Code auto-memory entrypoint behavior"
      pattern: "MEMORY\\.md"
---

<objective>
Integrate GSD phase memory writing with Claude Code's existing memdir system so completed phase outcomes and key decisions are recalled automatically across sessions.

Purpose: This delivers Phase 3's memory requirements without inventing a parallel store. GSD should write lean, durable phase memories exactly once per completed phase at verification/completion time.

Output: verify/complete flows write memdir-compatible `project` memories and auto-recall works through Claude Code's existing memory entrypoint behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
@.planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
@.planning/phases/03-plugin-packaging-and-memory/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a phase-memory writer at verification/completion time</name>
  <files>
    commands/verify-work/
    commands/complete-milestone/
    bin/gsd-tools.cjs
    bin/lib/state.cjs
  </files>
  <read_first>
    commands/verify-work/
    commands/complete-milestone/
    bin/gsd-tools.cjs
    bin/lib/state.cjs
    memdir/memoryTypes.ts
    memdir/paths.ts
    utils/claudemd.ts
    .planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Implement a concrete phase-memory write step that runs only when a phase is verified complete or when milestone completion needs to finalize a pending phase outcome. The written memory must be one file per completed phase and use the `project` memory type.

Use this exact content policy from context:
- include phase goal achievement
- include non-obvious decisions with rationale
- include surprising blocker resolutions
- exclude anything derivable from git history, code inspection, or CLAUDE.md

Write the memory file into Claude Code's auto-memory directory using the existing memdir path helpers. Also add or update the `MEMORY.md` entrypoint/index so Claude Code can auto-recall the memory in future sessions.
  </action>
  <verify>
    <automated>rg -n "project|MEMORY\\.md|auto-memory|memdir|Why:|How to apply:" commands/verify-work commands/complete-milestone bin/gsd-tools.cjs bin/lib/state.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `commands/verify-work` contains a phase-memory write step
    - `commands/complete-milestone` contains a phase-memory write or finalize step
    - The implementation writes a `project` memory file
    - The implementation updates or creates `MEMORY.md`
    - No implementation path writes memory during every plan or every hook event
  </acceptance_criteria>
  <done>Phase completion now writes one lean memdir-compatible project memory file per completed phase.</done>
</task>

<task type="auto">
  <name>Task 2: Make memory payload construction deterministic and non-derivable</name>
  <files>
    bin/lib/state.cjs
    bin/gsd-tools.cjs
    skills/
  </files>
  <read_first>
    bin/lib/state.cjs
    bin/gsd-tools.cjs
    memdir/memoryTypes.ts
    .planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
  </read_first>
  <action>
Create a single helper that assembles the phase memory payload from planning artifacts. It must pull only the durable fields needed for memory:
- phase identifier and goal
- achieved outcome
- non-obvious decisions with rationale
- surprising blocker resolution

Do not dump raw summaries, commit logs, or file inventories into memory. If a phase has no non-obvious decision or blocker, write only the durable outcome plus guidance.

Use a stable filename convention, for example `phase-03-plugin-packaging-and-memory.md`, so repeated completions update the same memory file instead of creating duplicates.
  </action>
  <verify>
    <automated>rg -n "phase-03|project|Why:|How to apply:|git log|files_modified" bin/lib/state.cjs bin/gsd-tools.cjs skills</automated>
  </verify>
  <acceptance_criteria>
    - The memory helper uses one stable filename per phase
    - The written memory payload includes `Why:` and `How to apply:`
    - The memory helper does not include raw `files_modified` inventories
    - The memory helper does not include git-log text dumps
  </acceptance_criteria>
  <done>GSD constructs lean, durable, non-derivable phase memories rather than copying planning artifacts verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Verify MEM-02 by proving recall comes from memdir, not manual bootstrapping</name>
  <files>
    memdir/paths.ts
    utils/claudemd.ts
    commands/verify-work/
    commands/complete-milestone/
  </files>
  <read_first>
    memdir/paths.ts
    utils/claudemd.ts
    commands/verify-work/
    commands/complete-milestone/
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Run an explicit recall audit showing that Phase 3 memory recall is satisfied by the existing Claude Code memdir pipeline:
1. the writer targets the auto-memory path helpers
2. the writer creates or updates `MEMORY.md`
3. no new CLAUDE.md bootstrap or manual "load context" step is introduced

If the implementation still requires a manual step, patch it before closing the plan.
  </action>
  <verify>
    <automated>rg -n "getAutoMemPath|MEMORY\\.md|CLAUDE\\.md|load context|manual" memdir/paths.ts utils/claudemd.ts commands/verify-work commands/complete-milestone</automated>
  </verify>
  <acceptance_criteria>
    - The implementation references `getAutoMemPath` or equivalent existing memdir helper
    - The implementation updates `MEMORY.md`
    - No new manual `CLAUDE.md` bootstrap step is introduced
    - No new custom memory store path outside memdir is introduced
  </acceptance_criteria>
  <done>MEM-02 is satisfied through Claude Code's memdir auto-recall behavior, not a custom bootstrap path.</done>
</task>

</tasks>

<verification>
- `rg -n "project|MEMORY\\.md|getAutoMemPath|Why:|How to apply:" commands/verify-work commands/complete-milestone bin/gsd-tools.cjs bin/lib/state.cjs`
- `rg -n "manual CLAUDE\\.md|load context" commands/verify-work commands/complete-milestone skills` returns no blocking manual bootstrap path
</verification>

<success_criteria>
GSD writes lean per-phase memories at completion time and Claude Code auto-recalls them through memdir. MEM-01 and MEM-02 are satisfied.
</success_criteria>
