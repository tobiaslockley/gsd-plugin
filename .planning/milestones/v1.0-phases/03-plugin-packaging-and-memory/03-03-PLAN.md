---
phase: 03-plugin-packaging-and-memory
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - hooks/hooks.json
  - mcp/server.cjs
  - .mcp.json
  - utils/sessionStart.ts
  - utils/plugins/mcpPluginIntegration.ts
  - utils/plugins/pluginLoader.ts
  - bin/gsd-tools.cjs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02

must_haves:
  truths:
    - "GSD hook behavior is provided by packaged plugin hooks instead of manual `~/.claude/settings.json` entries"
    - "The GSD MCP server is declared by the plugin manifest and runs from packaged plugin paths"
    - "The project no longer requires `.mcp.json` to point at `~/.claude/get-shit-done/mcp/server.cjs` for GSD to work"
    - "Plugin startup loads GSD hooks and MCP integration without duplicating hook declarations"
  artifacts:
    - path: "hooks/hooks.json"
      provides: "Packaged GSD hook declarations"
      contains: "\"hooks\""
    - path: "mcp/server.cjs"
      provides: "Packaged GSD MCP server"
      contains: "server"
    - path: ".mcp.json"
      provides: "Project-level config after GSD legacy removal"
      contains: ""
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "mcp/server.cjs"
      via: "manifest mcpServers declaration"
      pattern: "server\\.cjs"
    - from: "hooks/hooks.json"
      to: "utils/sessionStart.ts"
      via: "plugin hooks are loaded before SessionStart execution"
      pattern: "loadPluginHooks"
    - from: "hooks/hooks.json"
      to: "utils/plugins/pluginLoader.ts"
      via: "standard hooks/hooks.json auto-loading"
      pattern: "hooks/hooks\\.json"
---

<objective>
Move GSD's runtime integration points from ad hoc project/user config into the packaged plugin: hooks, MCP registration, and startup integration.

Purpose: Plugin installation is not complete until hooks and MCP are shipped as plugin components instead of manual `.mcp.json` and `~/.claude/settings.json` dependencies.

Output: packaged `hooks/hooks.json`, packaged `mcp/server.cjs`, and removal of GSD's legacy project `.mcp.json` dependency.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-plugin-packaging-and-memory/03-CONTEXT.md
@.planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
@.planning/phases/03-plugin-packaging-and-memory/03-01-PLAN.md
@.planning/phases/03-plugin-packaging-and-memory/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package GSD hooks in `hooks/hooks.json` and remove legacy settings dependence</name>
  <files>
    hooks/hooks.json
    bin/gsd-tools.cjs
  </files>
  <read_first>
    hooks/hooks.json
    utils/plugins/pluginLoader.ts
    utils/sessionStart.ts
    utils/hooks.ts
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Create or update `hooks/hooks.json` so the supported GSD hook behaviors ship inside the plugin. Use the standard wrapper structure:
- top-level `"description"`
- top-level `"hooks"`

Implement only the supported GSD hook behaviors that should survive Phase 3 packaging. Move them away from manual `~/.claude/settings.json` wiring. Use concrete hook commands that execute via:
`node "${CLAUDE_PLUGIN_ROOT}/bin/gsd-tools.cjs" ...`

Do not declare `hooks/hooks.json` again via `manifest.hooks`; the standard hooks file is auto-loaded by the plugin loader.
  </action>
  <verify>
    <automated>test -f hooks/hooks.json && rg -n "\"hooks\"|CLAUDE_PLUGIN_ROOT|gsd-tools\\.cjs" hooks/hooks.json</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/hooks.json` exists
    - `hooks/hooks.json` contains a top-level `"hooks"` key
    - `hooks/hooks.json` contains `${CLAUDE_PLUGIN_ROOT}/bin/gsd-tools.cjs`
    - `hooks/hooks.json` does not contain `~/.claude/hooks/`
    - No manifest field re-declares `hooks/hooks.json` through `manifest.hooks`
  </acceptance_criteria>
  <done>GSD hooks are packaged and auto-loadable through the plugin loader.</done>
</task>

<task type="auto">
  <name>Task 2: Move MCP startup from project-local `.mcp.json` to the plugin manifest</name>
  <files>
    mcp/server.cjs
    .mcp.json
    utils/plugins/mcpPluginIntegration.ts
  </files>
  <read_first>
    mcp/server.cjs
    .mcp.json
    utils/plugins/mcpPluginIntegration.ts
    .claude-plugin/plugin.json
    .planning/phases/03-plugin-packaging-and-memory/03-RESEARCH.md
  </read_first>
  <action>
Use the plugin manifest as the canonical MCP registration point for GSD. Keep `mcp/server.cjs` runnable from `${CLAUDE_PLUGIN_ROOT}/mcp/server.cjs` and remove the repo's reliance on `.mcp.json` pointing to a user-home server path.

Update `.mcp.json` by either:
- removing the GSD entry entirely if no longer needed, or
- replacing it with a non-GSD config that does not point at `~/.claude/get-shit-done/mcp/server.cjs`

The end state must make this statement true:
"A fresh plugin install provides GSD MCP without requiring repo-local `.mcp.json` bootstrapping."
  </action>
  <verify>
    <automated>rg -n "get-shit-done/mcp/server\\.cjs|CLAUDE_PLUGIN_ROOT|mcpServers" .mcp.json .claude-plugin/plugin.json mcp/server.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` contains a GSD `mcpServers` declaration
    - `mcp/server.cjs` exists
    - `.mcp.json` contains no reference to `~/.claude/get-shit-done/mcp/server.cjs`
    - `rg -n "get-shit-done/mcp/server\\.cjs" .mcp.json .claude-plugin/plugin.json mcp/server.cjs` returns no matches
  </acceptance_criteria>
  <done>GSD MCP startup is plugin-managed rather than project-bootstrapped from the legacy install.</done>
</task>

<task type="auto">
  <name>Task 3: Verify startup integration does not duplicate hooks or legacy paths</name>
  <files>
    utils/sessionStart.ts
    utils/plugins/pluginLoader.ts
    hooks/hooks.json
    .claude-plugin/plugin.json
  </files>
  <read_first>
    utils/sessionStart.ts
    utils/plugins/pluginLoader.ts
    hooks/hooks.json
    .claude-plugin/plugin.json
  </read_first>
  <action>
Run a concrete integration audit for startup behavior:
1. plugin hook loading still occurs through `loadPluginHooks()` before SessionStart execution
2. `hooks/hooks.json` is the only default packaged GSD hook source
3. no packaged hook or MCP config points at `~/.claude/hooks` or `~/.claude/get-shit-done`
4. there is no duplication between standard `hooks/hooks.json` loading and manifest hook declarations
  </action>
  <verify>
    <automated>rg -n "loadPluginHooks|hooks/hooks\\.json|~/.claude/hooks|~/.claude/get-shit-done" utils/sessionStart.ts utils/plugins/pluginLoader.ts hooks/hooks.json .claude-plugin/plugin.json .mcp.json</automated>
  </verify>
  <acceptance_criteria>
    - `utils/sessionStart.ts` still contains `loadPluginHooks`
    - `utils/plugins/pluginLoader.ts` still auto-loads `hooks/hooks.json`
    - `rg -n "~/.claude/hooks|~/.claude/get-shit-done" hooks/hooks.json .claude-plugin/plugin.json .mcp.json` returns no matches
    - The manifest does not duplicate `hooks/hooks.json` through `manifest.hooks`
  </acceptance_criteria>
  <done>GSD startup integration is plugin-native and free of duplicate or legacy hook wiring.</done>
</task>

</tasks>

<verification>
- `test -f hooks/hooks.json && test -f mcp/server.cjs`
- `rg -n "~/.claude/hooks|~/.claude/get-shit-done/mcp/server\\.cjs" hooks/hooks.json .claude-plugin/plugin.json .mcp.json mcp/server.cjs` returns no matches
- `rg -n "loadPluginHooks" utils/sessionStart.ts`
</verification>

<success_criteria>
GSD hooks and MCP now ship through the plugin itself. PLUG-01 and the install-runtime half of PLUG-02 are satisfied.
</success_criteria>
