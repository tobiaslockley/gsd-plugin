---
phase: 01-skill-and-agent-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/gsd/execute-phase.md
  - ~/.claude/commands/gsd/plan-phase.md
  - ~/.claude/commands/gsd/research-phase.md
  - ~/.claude/commands/gsd/verify-work.md
  - ~/.claude/commands/gsd/quick.md
  - ~/.claude/commands/gsd/debug.md
  - ~/.claude/commands/gsd/discuss-phase.md
  - ~/.claude/commands/gsd/map-codebase.md
  - ~/.claude/commands/gsd/new-project.md
  - ~/.claude/commands/gsd/new-milestone.md
  - ~/.claude/commands/gsd/validate-phase.md
  - ~/.claude/commands/gsd/review.md
  - ~/.claude/commands/gsd/autonomous.md
  - ~/.claude/commands/gsd/manager.md
  - ~/.claude/commands/gsd/ship.md
autonomous: true
requirements:
  - SKILL-01

must_haves:
  truths:
    - "All 15 orchestrator GSD commands have context: fork in their frontmatter"
    - "All 13 utility GSD commands do NOT have context: fork (remain inline)"
    - "GSD commands can still be invoked after frontmatter changes"
  artifacts:
    - path: "~/.claude/commands/gsd/execute-phase.md"
      provides: "Fork-mode orchestrator skill"
      contains: "context: fork"
    - path: "~/.claude/commands/gsd/plan-phase.md"
      provides: "Fork-mode orchestrator skill"
      contains: "context: fork"
    - path: "~/.claude/commands/gsd/quick.md"
      provides: "Fork-mode orchestrator skill"
      contains: "context: fork"
  key_links:
    - from: "~/.claude/commands/gsd/*.md"
      to: "SkillTool.ts executeForkedSkill()"
      via: "context: fork frontmatter field"
      pattern: "context: fork"
---

<objective>
Add `context: fork` frontmatter to all 15 GSD orchestrator commands so their orchestration prompts execute in isolated sub-agent contexts instead of polluting the parent conversation.

Purpose: Eliminates ~1-2K tokens of skill prompt content from the main conversation context per GSD command invocation. This is the foundation of SKILL-01 -- context isolation through Claude Code's built-in fork mechanism.

Output: 15 modified skill files in `~/.claude/commands/gsd/` with `context: fork` added to frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-and-agent-optimization/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add context: fork to all 15 orchestrator GSD commands</name>
  <files>
    ~/.claude/commands/gsd/execute-phase.md
    ~/.claude/commands/gsd/plan-phase.md
    ~/.claude/commands/gsd/research-phase.md
    ~/.claude/commands/gsd/verify-work.md
    ~/.claude/commands/gsd/quick.md
    ~/.claude/commands/gsd/debug.md
    ~/.claude/commands/gsd/discuss-phase.md
    ~/.claude/commands/gsd/map-codebase.md
    ~/.claude/commands/gsd/new-project.md
    ~/.claude/commands/gsd/new-milestone.md
    ~/.claude/commands/gsd/validate-phase.md
    ~/.claude/commands/gsd/review.md
    ~/.claude/commands/gsd/autonomous.md
    ~/.claude/commands/gsd/manager.md
    ~/.claude/commands/gsd/ship.md
  </files>
  <read_first>
    ~/.claude/commands/gsd/execute-phase.md
    ~/.claude/commands/gsd/plan-phase.md
    ~/.claude/commands/gsd/research-phase.md
    ~/.claude/commands/gsd/verify-work.md
    ~/.claude/commands/gsd/quick.md
    ~/.claude/commands/gsd/debug.md
    ~/.claude/commands/gsd/discuss-phase.md
    ~/.claude/commands/gsd/map-codebase.md
    ~/.claude/commands/gsd/new-project.md
    ~/.claude/commands/gsd/new-milestone.md
    ~/.claude/commands/gsd/validate-phase.md
    ~/.claude/commands/gsd/review.md
    ~/.claude/commands/gsd/autonomous.md
    ~/.claude/commands/gsd/manager.md
    ~/.claude/commands/gsd/ship.md
    .planning/phases/01-skill-and-agent-optimization/01-RESEARCH.md
  </read_first>
  <action>
For each of these 15 orchestrator command files, add `context: fork` as a frontmatter field inside the existing YAML frontmatter block (between the `---` markers). Place it after the `description:` line and before `argument-hint:` or `allowed-tools:` (whichever comes first). If neither exists, place after `description:`.

The exact line to add is:
```
context: fork
```

**Commands to modify (all 15):**

1. `execute-phase.md` -- Add `context: fork` after `argument-hint:` line
2. `plan-phase.md` -- Already has `agent: gsd-planner`. Add `context: fork` after `argument-hint:` line, before `agent:`
3. `research-phase.md` -- Add `context: fork` after `argument-hint:` line
4. `verify-work.md` -- Add `context: fork` after `argument-hint:` line
5. `quick.md` -- Add `context: fork` after `argument-hint:` line
6. `debug.md` -- Add `context: fork` after `argument-hint:` line
7. `discuss-phase.md` -- Add `context: fork` after `argument-hint:` line
8. `map-codebase.md` -- Add `context: fork` after `argument-hint:` line
9. `new-project.md` -- Add `context: fork` after `argument-hint:` line
10. `new-milestone.md` -- Add `context: fork` after `argument-hint:` line
11. `validate-phase.md` -- Add `context: fork` after `argument-hint:` line
12. `review.md` -- Add `context: fork` after `argument-hint:` line
13. `autonomous.md` -- Add `context: fork` after `argument-hint:` line
14. `manager.md` -- Add `context: fork` after `description:` line (no argument-hint)
15. `ship.md` -- Add `context: fork` after `argument-hint:` line

**Commands to NOT modify (utility commands that stay inline):**
help.md, progress.md, stats.md, settings.md, health.md, next.md, note.md, add-todo.md, check-todos.md, resume-work.md, pause-work.md, session-report.md, join-discord.md

Also do NOT add `context: fork` to these non-orchestrator commands: add-backlog.md, add-phase.md, add-tests.md, audit-milestone.md, audit-uat.md, cleanup.md, complete-milestone.md, do.md, fast.md, forensics.md, insert-phase.md, list-phase-assumptions.md, list-workspaces.md, milestone-summary.md, new-workspace.md, plan-milestone-gaps.md, plant-seed.md, pr-branch.md, profile-user.md, reapply-patches.md, remove-phase.md, remove-workspace.md, review-backlog.md, set-profile.md, thread.md, ui-phase.md, ui-review.md, update.md, workstreams.md

**Anti-pattern to avoid:** Do NOT add `context: fork` to lightweight utility commands (help, progress, stats, etc.). These only read/display information and forking would add unnecessary sub-agent spawn overhead (~2-5 seconds) for simple lookups.

**Caution:** For `plan-phase.md` which already has `agent: gsd-planner` in frontmatter, adding `context: fork` is still correct -- the fork isolates the orchestrator prompt, and the `agent:` field specifies which agent type runs the forked execution.
  </action>
  <verify>
    <automated>for f in execute-phase plan-phase research-phase verify-work quick debug discuss-phase map-codebase new-project new-milestone validate-phase review autonomous manager ship; do grep -l "context: fork" ~/.claude/commands/gsd/$f.md || echo "MISSING: $f.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 15 files contain `context: fork` in their YAML frontmatter block
    - grep -c "context: fork" across all 15 files returns 15
    - help.md, progress.md, stats.md, next.md do NOT contain "context: fork"
    - All modified files still have valid YAML frontmatter (opening and closing `---` markers intact)
    - No other frontmatter fields were removed or altered
  </acceptance_criteria>
  <done>All 15 orchestrator GSD commands have `context: fork` in frontmatter. Utility commands remain inline. Frontmatter is valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no utility commands were accidentally forked</name>
  <files>None (read-only verification)</files>
  <read_first>
    ~/.claude/commands/gsd/help.md
    ~/.claude/commands/gsd/progress.md
    ~/.claude/commands/gsd/stats.md
    ~/.claude/commands/gsd/next.md
    ~/.claude/commands/gsd/note.md
    ~/.claude/commands/gsd/settings.md
  </read_first>
  <action>
Run verification commands to confirm:
1. Exactly 15 GSD command files contain `context: fork`
2. None of the 13 utility commands (help, progress, stats, settings, health, next, note, add-todo, check-todos, resume-work, pause-work, session-report, join-discord) contain `context: fork`
3. All modified files have valid YAML frontmatter (both opening and closing `---` markers present)

Run these exact commands:
```bash
# Count forked commands (should be exactly 15)
grep -rl "context: fork" ~/.claude/commands/gsd/ | wc -l

# List forked commands for visual audit
grep -rl "context: fork" ~/.claude/commands/gsd/ | sort

# Verify utility commands are NOT forked
for f in help progress stats settings health next note add-todo check-todos resume-work pause-work session-report join-discord; do
  if grep -q "context: fork" ~/.claude/commands/gsd/$f.md 2>/dev/null; then
    echo "ERROR: $f.md should NOT have context: fork"
  fi
done

# Verify YAML frontmatter integrity (each file starts with --- and has a second ---)
for f in execute-phase plan-phase research-phase verify-work quick debug discuss-phase map-codebase new-project new-milestone validate-phase review autonomous manager ship; do
  lines=$(grep -c "^---$" ~/.claude/commands/gsd/$f.md)
  if [ "$lines" -lt 2 ]; then
    echo "ERROR: $f.md has broken frontmatter (found $lines --- markers, need >= 2)"
  fi
done
```
  </action>
  <verify>
    <automated>count=$(grep -rl "context: fork" ~/.claude/commands/gsd/ | wc -l | tr -d ' '); test "$count" = "15" && echo "PASS: $count forked commands" || echo "FAIL: expected 15, got $count"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl "context: fork" ~/.claude/commands/gsd/ | wc -l returns exactly 15
    - No utility command file (help.md, progress.md, stats.md, settings.md, health.md, next.md, note.md, add-todo.md, check-todos.md, resume-work.md, pause-work.md, session-report.md, join-discord.md) contains "context: fork"
    - All 15 modified files have at least 2 `---` markers (valid YAML frontmatter)
  </acceptance_criteria>
  <done>Exactly 15 orchestrator commands are forked, 0 utility commands are forked, all frontmatter is valid.</done>
</task>

</tasks>

<verification>
- `grep -rl "context: fork" ~/.claude/commands/gsd/ | wc -l` returns 15
- `grep -rl "context: fork" ~/.claude/commands/gsd/ | sort` lists exactly: autonomous, debug, discuss-phase, execute-phase, manager, map-codebase, new-milestone, new-project, plan-phase, quick, research-phase, review, ship, validate-phase, verify-work
- No utility commands contain `context: fork`
</verification>

<success_criteria>
All 15 GSD orchestrator commands execute in isolated sub-agent contexts via `context: fork` frontmatter. Utility commands remain inline for fast execution. SKILL-01 requirement is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-and-agent-optimization/01-01-SUMMARY.md`
</output>
